# Source

Reusable implementations live here: kernels, framework integration,
runtime helpers, graph passes, model-system components, training utilities,
and inference-serving pieces.

Keep throwaway experiments in `experiments/` until they are stable enough
to reuse.
