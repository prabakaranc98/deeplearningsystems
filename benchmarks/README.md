# Benchmarks

Put reproducible measurement code and benchmark reports here.

Good benchmark entries should include:

- hardware and software environment;
- command used to run the benchmark;
- warmup and repetition settings;
- latency, throughput, memory, or scaling numbers;
- comparison against a reference implementation;
- interpretation of what the numbers mean.

Prefer small, repeatable benchmarks before large experiments.
